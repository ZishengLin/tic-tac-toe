#ifndef MINMAX_STRATEGY_H_
#define MINMAX_STRATEGY_H_

int next_best_move(int* arr);
int minmax_strategy(int* arr, int depth, int type);
int get_max(int a, int b);
int get_min(int a, int b);

#endif /* MINMAX_STRATEGY_H_ */